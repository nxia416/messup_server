<h1>Listing iptable_logs</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>User data</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @iptable_logs.each do |iptable_log| %>
      <tr>
        <td><%= iptable_log.user_id %></td>
        <td><%= iptable_log.user_data %></td>
        <td><%= link_to 'Show', iptable_log %></td>
        <td><%= link_to 'Edit', edit_iptable_log_path(iptable_log) %></td>
        <td><%= link_to 'Destroy', iptable_log, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Iptable log', new_iptable_log_path %>
